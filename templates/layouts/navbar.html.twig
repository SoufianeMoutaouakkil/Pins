<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">My Website</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('pin_index') }}">Manage your Pins</a>
                </li>
                </ul>
                <ul class="navbar-nav me-1 ms-auto">
                <li class="nav-item">
                    <a class="btn btn-outline-danger me-2" href="{{ path('app_sec_logout') }}">Logout</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-outline-primary" href="{{ path('user_profile') }}">{{ app.user.username}}</a>
                </li>
                </ul>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_sec_login') }}">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_sec_register') }}">Register</a>
                    </li>
                    </ul>
                {% endif %}
        </div>
    </div>
</nav>
